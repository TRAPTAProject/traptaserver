With QtCreator, make release (foldler build-...release)

cd this folder

$ /path/to/Qt/5.12.9/clang_64/bin/macdeployqt TRAPTA.app -qmldir=/path/to/Qt/5.10.1/clang_64/qml
Remove unused frameworks, plugins and qml 
Make dmg from app with disk utility.

Here are the list of mandatory frameworks and plugins :

